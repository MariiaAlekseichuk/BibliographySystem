'use strict';
var BibliographySystemApp = {};

var app = angular.module('BibliographySystemApp', ['ngRoute',  'ui.router']);

app.run(['$state', '$stateParams',
    function($state, $stateParams) {
    //this solves page refresh and getting back to state
    }
]);